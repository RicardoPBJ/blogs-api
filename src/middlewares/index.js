const loginVerify = require('./loginVerify');
const userVerify = require('./userVerify');
const categoriesVerify = require('./categoriesVerify');
const postVerify = require('./postVerify');

module.exports = {
  loginVerify,
  userVerify,
  categoriesVerify,
  postVerify,
};